<!DOCTYPE html>
<html lang="en">
<head>
    <title>Record</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet"  href="/css/header.css">
    <link rel="stylesheet"  href="/css/buttons.css">
    <link rel="stylesheet"  href="/css/logout.css">
</head>
<body>
    
    <%- include("common/header"); %>

    <a href="http://localhost:4200/student" class="mx-4"><b><-Back</b></a>

    <% if(studentData!=undefined) { %>
    <div class="container mt-5" style="width:50%; border: 2px solid black;">
        <div class="d-flex justify-content-between mb-3 mt-4">
          <div></div>
          <h3>Result</h3>
          <div></div>
        </div>
           <div class="row d-flex justify-content-center mb-3 mt-4">
              <div class="col-md-4">
                <div class="d-flex justify-content-end">
                  <p class="text-right">Roll No :</p>
                </div>
                <div class="d-flex justify-content-end">
                  <p class="text-right">Name :</p>
                </div>
                <div class="d-flex justify-content-end">
                  <p class="text-right">Date of Birth :</p>
                </div>
                <div class="d-flex justify-content-end">
                  <p class="text-right">Score :</p>
                </div>   
              </div>
              <div class="col-md-4 mx-4">
                <p><%= studentData.rollNo %></p>
                <p><%= studentData.studentName %></p>
                <p><%= studentData.birth %></p>
                <p><%= studentData.score %></p>
              </div>
          </div>
      </div>
      <% }  else{ %>
        <div class="container d-flex justify-content-between" style="margin-top:120px;">
          <div></div>
          <div>
            <h2 style="color:red">Record Not Found</h2>
          </div>
          <div></div>
        </div> 
      <% } %>

</body>
</html>